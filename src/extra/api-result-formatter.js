export function getFormattedRadarsData(rawData, reportConfiguration) {
    /* TODO bisogna finire la formattazione dei rawData nel progetto situato estarnamente a questa cartella */
  console.log(rawData);
}
